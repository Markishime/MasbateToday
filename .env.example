

NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# -----------------------------------------------------------------------------
# Admin access (Optional)
# -----------------------------------------------------------------------------
# Email that can sign in to /admin. If unset, default is fel.monares@masbatetoday.com
# Admin users must also exist in Firestore collection "admins" (document ID = user uid).

NEXT_PUBLIC_ADMIN_EMAIL=admin@example.com

# -----------------------------------------------------------------------------
# Weather API (Optional – for live weather on homepage)
# -----------------------------------------------------------------------------
# Where to get: https://openweathermap.org/api
# 1. Sign up for a free account
# 2. API keys → Create key (free tier: 1000 calls/day)
# If missing, the site shows fallback static weather.

NEXT_PUBLIC_WEATHER_API_KEY=

# -----------------------------------------------------------------------------
# Google AdSense (Optional – for ads)
# -----------------------------------------------------------------------------
# Where to get: https://www.google.com/adsense/
# 1. Sign in → Account → Settings → Access and authorization
# 2. Use your "Ads by Google" client ID (format: ca-pub-XXXXXXXXXXXXXXXX)
# If missing, ad components won’t render.

NEXT_PUBLIC_ADSENSE_CLIENT_ID=

# -----------------------------------------------------------------------------
# Google Maps (Optional – for maps and places)
# -----------------------------------------------------------------------------
# Where to get: https://console.cloud.google.com/apis/credentials
# 1. Create a project or select one
# 2. Enable "Maps JavaScript API" and "Places API"
# 3. Create credentials → API key → restrict by HTTP referrer in production
# If missing, map components will show a message instead of the map.

NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# -----------------------------------------------------------------------------
# Stripe (Optional – for premium or payments)
# -----------------------------------------------------------------------------
# Where to get: https://dashboard.stripe.com/apikeys
# Use "Publishable key" (starts with pk_test_ or pk_live_).
# If you don’t use payments, leave this unset.

# -----------------------------------------------------------------------------
# Support / Donations (GCash only – for Support Masbate Today page)
# -----------------------------------------------------------------------------
# Set your public GCash number here; it will be shown on the Support page and
# copied to the visitor's clipboard when they click "Copy GCash Number".
# Example:
# NEXT_PUBLIC_GCASH_NUMBER=09XXXXXXXXX

NEXT_PUBLIC_GCASH_NUMBER=

# -----------------------------------------------------------------------------
# Email / SMTP (Optional – for sending booking emails)
# -----------------------------------------------------------------------------
# Free method: you can use your Gmail account with an app password.
# 1. In Gmail → Manage account → Security → App passwords
# 2. Create an app password for "Mail" and "Other"
# 3. Use smtp.gmail.com, port 587, your Gmail address, and the app password
# If these are set, booking requests will be sent directly via SMTP.
#
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=yourgmail@gmail.com
# SMTP_PASS=your_app_password_here

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=marklloydcuizon@gmail.com 
SMTP_PASS=onxgadbhjvpejqvv
